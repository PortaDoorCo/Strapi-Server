{
  "routes": [
    {
      "method": "GET",
      "path": "/finishes",
      "handler": "finish.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/finishes/count",
      "handler": "finish.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/finishes/:id",
      "handler": "finish.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/finishes",
      "handler": "finish.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/finishes/:id",
      "handler": "finish.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/finishes/:id",
      "handler": "finish.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
